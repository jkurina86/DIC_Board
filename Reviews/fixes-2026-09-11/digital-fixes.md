> Final integration: root ERC is clean. Shared project libraries are synchronized; RTC bias labels now use global labels; fabricated TP20–TP26 are excluded from BOM; power references are unique; C100/C41/C113 now display polarity. The integrated netlist and validation in this folder supersede intermediate snapshots mentioned below.

# Digital corrections — 2026-09-11

Edited only `DIC_Board/mcu.kicad_sch`, `DIC_Board/comms.kicad_sch` and `Firmware/DIC/DIC.ioc`. Baselines exist in this directory and root's `baseline/`. No generated firmware or shared symbol-library file was written by this reviewer.

## Implemented

- U39 now uses project-local `MAX3226EETE_ReviewFixed`, physical TQFN numbering and grounded EP17. Ordering MPN/value is `MAX3226EETE+`. The exact manufacturer mapping includes C1+16 and V+15; this exception to a simple cyclic renumbering was caught and corrected during visual verification. The final netlist independently checks all17 pins.
- C43 adds1uF from the downstream FB1/VREF+ net toGND beside C27. TDK `C1608X7R1E105K080AB`,0603,25V,X7R,10%. C27 stays100nF. The old decoupling caption incorrectly assigning upstream C18/C19 toVREF+ is corrected.
- C21 changes2.2nF→100nF with matching MPN `C1608X7R1H104K080AA`,0603,50V,X7R.
- R348 holds COMM_EN low in reset. R349/R350 hold RTC_CE/RTC_CLKOE low. All100k,1%,0603,Vishay `CRCW0603100KFKEA`.
- Added42 MCU unused-GPIO no-connect markers based on the authoritative root XML and the absence of a matching IOC function. Added J2 key/TDI no-connects; no connected function was removed.
- Added honest PWR_FLAG declarations on the RTC backup feed afterR3 and debugger VTref afterR19, documenting power that comes through those passive resistors.
- D1/D2 and J1 retain their intentional exact-package pin functions under project-owned symbols `RCLAMP0582B_ReviewFixed` and `DM3AT_SF_PEJM5_ReviewFixed`. Matching filters and library snippets are supplied; root will synchronize the shared library.
- Removed an inherited redundant1.27mm TIN wire stub, leaving the existing connection unchanged.
- Set explicit, documented CubeMX RCC values:8MHz HSE→mainPLL N8/M1/R4=16MHz SYSCLK/HCLK/APBs; separate PLLSAI1 N12/M1/Q2=48MHz USB/SDMMC. This preserves the16MHz peripheral rate previously recorded by the SPI/PLL cache values and removes the prior ambiguity of an implicit MSI default. SPI1 remains125kbit/s at prescaler128. RCC keys/enums were checked against installed CubeMX6.15 STM32L4 RCC XML. Explicitly disabled hardware VBUS sensing because PA9 is USART1_TX and PE0 is the software VBUS input.

## Expected net changes

- Only new physical nodes: C43.1 VREF+, C43.2 GND; R348.1 COMM_EN,R348.2 GND; R349.1 RTC_CE,R349.2 GND; R350.1 RTC_CLKOE,R350.2 GND. Power flags are electrical declarations, not BOM components.
- U39 old→new:1→14,2→16,3→15,4→1,5→2,6→3,7→4,8→5,9→6,10→7,11→8,12→9,13→10,14→11,15→12,16→13; added17 GND.
- All U1/U2/U3/J1/J2/P1/D1/D2/Y1/Y2 pin-to-net maps remain identical to the original resolved review export. Netlist proof is in `digital-verification.json`, generated by `verify_digital_fixes.py` against `digital-final.net.xml`.

## Verification and remaining acceptance work

KiCad10 successfully parsed/exported both edited schematics and the authoritative hierarchy. Independently checked all U39 pins, all unchanged MCU/interface pinmaps, all four new component nets, and clock calculations. Rendered and visually inspected MCU and communications sheets. The intermediate hierarchy ERC had17 findings, primarily pending shared-library synchronization and other owners' changes; root performs the final ERC and global net-diff check. This is not a claim that the intermediate ERC was clean.

LSE values remain6.8pF because no PCB/parasitic model exists from which a defensible replacement could be selected. Added explicit effective-load limits and qualification instructions. Its gain/startup/drive and the HSE oscillator require layout/temperature testing; a numerical capacitor change would not establish qualification.

The IOC configuration is checked against the installed database and arithmetic, but a CubeMX GUI reload/generation was not performed. No firmware source exists by instruction. The future application must gate USB attach/detach using PE0, implement PSRAM initialization/refresh-safe transaction behavior and relevant STM32 errata, and select RTC trickle charging appropriate to the battery chemistry. These runtime requirements cannot be implemented solely as IOC parameters.

BT1,J2,JP1,P1,SW1 remain the user's explicitly deferred mechanical/hardware choices. No arbitrary replacements were selected.
